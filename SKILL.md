---
name: python-quality-review
description: 通用 Python 项目代码质量与架构审查技能。从代码风格、封装边界、高内聚低耦合、游离方法、架构设计、测试质量六大维度做系统性审查，输出结构化质量报告。当用户说"代码质量审查"、"全面审查"、"架构审查"、"质量检查"、"code review"、"review 代码"、"质量报告"、"游离方法检查"、"封装边界检查"、"工程规范对标"、"技术债务盘点"时使用此技能。支持快速模式（单模块扫描）和深度模式（全维度 + 改进路线图）。
keywords:
    - 代码质量
    - 质量审查
    - 架构审查
    - 全面审查
    - code review
    - 质量报告
    - 工程规范
    - 高内聚低耦合
    - 游离方法
    - 封装边界
    - 代码坏味道
    - 设计模式
    - 重构建议
    - 技术债务
    - quality audit
    - code quality
    - architecture review
---

# Python 代码质量审查技能

综合成熟开源项目的工程实践与经典代码坏味道（code smell）识别体系，对 Python 项目做系统性质量审查。

## Overview

- **适用场景**：代码质量审查、架构审查、封装边界检查、游离方法分析、PR review、技术债务盘点
- **审查维度**：代码风格、封装边界、高内聚低耦合、游离方法、架构与设计模式、测试质量（共 6 大维度）
- **输出格式**：结构化 Markdown 报告，按 P0/P1/P2/P3 分级，含具体位置 + 问题描述 + 修复建议
- **两种模式**：快速模式（默认，3 个核心维度 + 精简报告）/ 深度模式（6 个维度全做 + 改进路线图）

## 统一审查入口

用户说「全面审查」「全部检查」「完整质量审查」「review 整个项目」等，即启动**深度模式全维度审查**，一次性输出完整报告。

典型触发句：
- 「对当前项目做全面质量审查」
- 「完整 review 一下代码质量」
- 「审查所有维度」
- 「做一次深度质量审计」

深度模式必须覆盖全部 6 个维度，缺一不可。

## 审查流程（六步法）

> **第 0 步·自动预扫（必做）**：先跑零依赖脚本客观锁定问题，再人工深读。
>
> ```bash
> python scripts/preflight.py <审查范围> --min-severity P1
> # 或要全量： python scripts/preflight.py <范围> --json
> ```
> 脚本自动报出：猴子补丁、裸 except、跨对象 `_private` 穿透、跨模块导入内部符号、
> 上帝类、假类、超长函数、if-elif 分发链等。**把 P0/P1 结论直接纳入报告，再对命中点逐一人工复核**
> （静态报的可能是误报，人工确认后再定级）。详见 `scripts/README.md`。

```
┌─────────────────────────────────────────────────────┐
│  1. 范围确认                                          │
│     - 确认审查范围（单文件 / 单模块 / 全项目）          │
│     - 确认模式（快速 / 深度）                          │
│     - 跑 scripts/preflight.py 自动预扫，锁定客观问题    │
├─────────────────────────────────────────────────────┤
│  2. 代码风格检查（python-refactoring 坏味道体系）      │
│     - 过长函数 / 上帝类 / 重复代码 / 魔法数字          │
│     - 类型注解覆盖率 / 命名规范 / docstring           │
│     - 异常处理规范（裸 except、日志记录）              │
├─────────────────────────────────────────────────────┤
│  3. 封装与边界检查（公共/内部边界）                    │
│     - 私有属性穿透（_xxx 被外部访问）                  │
│     - 公共/内部边界（__all__、_ 前缀模块）             │
│     - 猴子补丁 / 动态挂载方法                         │
├─────────────────────────────────────────────────────┤
│  4. 高内聚低耦合 + 游离方法分析                        │
│     - 游离函数（操作某对象但定义在对象外）              │
│     - 跨模块直操内部状态                              │
│     - 假类（全 staticmethod 的命名空间类）             │
│     - 模块职责是否单一                                │
├─────────────────────────────────────────────────────┤
│  5. 架构与设计模式检查                                │
│     - 基类设计（模板方法、抽象契约）                   │
│     - 扩展点配套完整性（实现 + 注册 + 测试 + 文档）     │
│     - if-elif 分发链 vs 注册表/多态                   │
│     - 异常体系分层                                    │
├─────────────────────────────────────────────────────┤
│  6. 测试质量 + 输出报告                               │
│     - 测试覆盖、测试真实性、慢用例/flaky               │
│     - 按 P0/P1/P2/P3 分级输出                         │
│     - 深度模式附改进路线图                             │
└─────────────────────────────────────────────────────┘
```

## 审查维度详解

### 维度一：代码风格（参考 `references/code-style-checklist.md`）

| 检查项 | 标准 | 工具 |
|--------|------|------|
| 类型注解 | 公共方法 100% 覆盖参数 + 返回值 | mypy `--disallow-untyped-defs` |
| 命名规范 | 类 PascalCase、函数 snake_case、内部 `_` 前缀 | ruff |
| Docstring | 公共 API 必须有 Google 风格完整 docstring | pydocstyle |
| 行宽 | 代码行 ≤ 88 字符（CJK docstring 可适度放宽） | ruff E501 |
| 异常处理 | 禁止裸 `except:`，异常必须记录日志 | 人工审查 |
| 导入顺序 | 标准库 → 第三方 → 本地 | ruff isort |

### 维度二：封装边界（参考 `references/encapsulation-checklist.md`）

**红线（零容忍）**：
- ❌ 直接访问其他类的 `_` 前缀属性（`obj._private_attr`）
- ❌ 用 `# noqa: SLF001` 压制封装告警
- ❌ 猴子补丁动态挂载方法到类上（`Class.method = func`）
- ❌ 修改其他模块的全局变量

**黄线（需关注）**：
- ⚠️ `__init__.py` 缺少显式 `__all__`
- ⚠️ 内部模块没有 `_` 前缀但也不导出
- ⚠️ 类的公共属性过多（应通过 property 暴露派生值）

### 维度三：高内聚低耦合 + 游离方法（参考 `references/cohesion-coupling-checklist.md`）

**游离方法判定标准**（满足任一即可能是游离）：
1. 模块级函数，第一个参数是某类实例，且操作该实例的属性/方法
2. 自由函数 + 猴子补丁挂到类上
3. 放在 utils/ 但只被一个类/模块使用，逻辑上属于那个类
4. A 模块函数直接读写 B 模块对象的 `_` 前缀属性（行为放错了地方）

**高内聚判定**：
- 一个类/模块只做一件事，修改它只有一个理由
- 类的方法使用的属性高度重叠（LCOM 指标低）
- 模块内的函数/类之间关联性强

**低耦合判定**：
- 模块之间通过公共 API 交互，不碰内部状态
- 依赖方向清晰（上层依赖下层抽象，不依赖具体实现）
- 修改一个模块不需要联动修改很多其他模块

### 维度四：架构与设计模式（参考 `references/architecture-checklist.md`）

**基类设计质量**：
- ✅ 基类声明了完整的能力契约（抽象方法 + 属性）
- ✅ 公共逻辑在基类，子类只实现差异化钩子（模板方法）
- ✅ 嵌套配置类内聚参数
- ❌ 基类是空壳（所有逻辑都在子类重复）

**扩展点质量**：
- ✅ 新增一种扩展有明确的配套清单
- ✅ 注册机制显式（注册表 / 工厂），不靠自动扫描魔法
- ❌ 新增扩展需要改 N 个地方（散弹式修改）

**异常体系**：
- ✅ 按"面向谁处理"分层（运行时可恢复 vs 开发者错误）
- ✅ 自定义异常 + 统一错误码枚举
- ❌ 到处 `raise RuntimeError` / `raise Exception`

### 维度五：测试质量

- ✅ 测试验证真实行为，不是 mock 一切
- ✅ 边界情况有覆盖（空输入、异常路径、并发）
- ✅ 集成测试走真实通路
- ❌ 核心模块（>300 行）无专用单元测试
- ❌ 测试用 sleep 轮询代替事件同步（慢且 flaky）

### 维度六：依赖与配置（快速模式可跳过）

- ✅ 可选依赖分组管理，不用不装
- ✅ 可选依赖惰性导入，ImportError 提示清晰
- ✅ 配置集中管理，类型安全
- ❌ 配置项散落各处，无统一入口

## 报告输出格式

快速模式用精简格式，深度模式用完整格式。详见 `references/report-template.md`。

### 快速模式输出结构

```markdown
# 代码质量审查：[模块/项目名]

> 审查范围：[路径]
> 审查模式：快速
> 审查日期：YYYY-MM-DD

## 总体评价
**质量评级：[优秀 / 良好 / 中等 / 待改进]**

### ✅ 做得好的
- ...

### 🔴 主要问题
- ...

## 问题清单
### 🔴 P0 - 必须修复
| # | 类型 | 位置 | 问题 | 建议 |
### 🟠 P1 - 应该修复
| ... |
### 🟡 P2 - 可选优化
| ... |
```

### 深度模式输出结构

完整六维度 + 专项分析 + 改进路线图。模板见 `references/report-template.md`。

## 快速模式 vs 深度模式

| 维度 | 快速模式（默认） | 深度模式 |
|------|-----------------|---------|
| 代码风格 | ✅ 核心检查 | ✅ 完整检查 |
| 封装边界 | ✅ 核心检查 | ✅ 完整检查 |
| 高内聚低耦合 + 游离方法 | ✅ 核心检查 | ✅ 完整检查 |
| 架构与设计模式 | — | ✅ |
| 测试质量 | — | ✅ |
| 依赖与配置 | — | ✅ |
| 改进路线图 | — | ✅ |
| 适用场景 | 单模块/快速扫描 | 全项目/架构级/正式报告 |

**触发深度模式的关键词**：全面、深度、完整、详细、架构级、全项目、审计、盘点。

**默认行为**：用户不说深度/全面，就用快速模式；但如果审查范围是整个项目或说"全部维度"，自动升级为深度模式。

## 参考文档

- `references/code-style-checklist.md` — 代码风格检查清单
- `references/encapsulation-checklist.md` — 封装边界审查清单
- `references/cohesion-coupling-checklist.md` — 高内聚低耦合 + 游离方法检查清单
- `references/architecture-checklist.md` — 架构与设计模式审查清单
- `references/best-practices-benchmarks.md` — 工程规范对标参考（以 AgentScope 为范例）
- `references/report-template.md` — 质量审查报告完整模板（快速/深度双模式）

## 与其他技能的关系

- **python-refactoring**：本技能在"代码风格"维度复用其坏味道识别体系。python-refactoring 侧重"动手重构代码"，本技能侧重"审查 + 出报告 + 路线图"。
- **code-review**：通用 code review 技能侧重功能正确性和 bug 发现。本技能侧重**架构质量 + 工程规范 + 设计模式**，是更偏架构和工程化的审查。
- **security-audit**：安全审查是独立维度。本技能只在封装边界部分顺带检查安全相关的封装问题，不替代安全审计。
- **simplify**：simplify 侧重代码精简和复用。本技能是系统性全维度审查，范围更广。
